# MLXBits Image Studio — hub Proxmox (Dokploy)
# Contexte de build = racine du repo (contexte git direct).
services:
  studio-hub:
    build:
      context: https://github.com/appweb514-svg/image-studio-remote.git#main
      dockerfile: hub/hub-service/Dockerfile
    container_name: studio-hub
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      WORKER_URL: http://100.102.122.144:8899
      WORKER_TOKEN: klein-4b
      ADMIN_USER: gildas
      ADMIN_PASSWORD: Studio2026!
      STUDIO_DATA: /data
      WEBUI_DIST: /srv/webui
    volumes:
      - studio-data:/data

volumes:
  studio-data:
